<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>Authorization</title>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.1/dist/css/bootstrap.min.css" rel="stylesheet">
        <script>
            function redirect_toReg(){window.location.href=__dirname + "/registration.html";}
        </script>
    </head>
    <body class="bg-dark">
        {{#if error}}
            <div class="alert alert-danger alert-dismissible fade show" role="alert">
                <strong>Data was entered incorrectly or this account has been blocked</strong>
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            </div>
        {{/if}}
        {{#if error_entry}}
            <div class="alert alert-danger alert-dismissible fade show" role="alert">
                <strong>One or more fields are not filled in</strong>
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            </div>
        {{/if}}        
        <div class="position-absolute top-50 start-50 translate-middle text-white">
            <form method="post">
                <div class="text-center mb-3 pb-1 fs-3 border-bottom">Authorization</div>
                <div class="mb-3">
                  <label for="Email" class="form-label">Email address</label>
                  <input type="email" name="email" class="form-control" id="Email">
                </div>
                <div class="mb-3">
                  <label for="Password" class="form-label">Password</label>
                  <input type="password" name="password" class="form-control" id="Password">
                </div>
                <div class="btn-group pt-2">
                    <button type="submit" class="btn btn-outline-light fs-5 shadow-none">Authorization</button>
                    <button onclick='window.location = "/registration";' type="button" class="btn btn-outline-light fs-5 shadow-none">Registration</button>
                </div>
              </form>
        </div>
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.1/dist/js/bootstrap.bundle.min.js"></script>
    </body>
</html>